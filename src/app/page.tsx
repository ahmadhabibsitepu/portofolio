import CV from "@/components/cv/CV";

const Page = () => {
  return (
    <main className="flex justify-center w-full">
      <section className="border-2">
        <CV></CV>
      </section>
    </main>
  );
};

export default Page;
